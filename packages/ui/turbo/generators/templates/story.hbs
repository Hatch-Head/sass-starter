import type { Meta, StoryObj } from "@storybook/react";
import Components from "./{{ name }}";

const meta: Meta<typeof Components> = {
  component: Components,
};

export default meta;
type Story = StoryObj<typeof Components>;

export const {{ ufirst name }}: Story = {

};
